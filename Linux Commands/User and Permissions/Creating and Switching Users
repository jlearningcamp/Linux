sudo useradd <uname>
sudo passwd <uname>

su uname 
- switch to a particular user

sudo groupadd <groupname>

sudo usermod -aG <group> <user>
- Add a user to a group

sudo userdel/groupdel <uname or gp.name>
